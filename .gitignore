# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets & local runtime data
.env
.env.*
*.db
*.sqlite
*.sqlite3
backups/
orion-data-backup*/

# Local tool state
.claude/
.cursor/
# but keep the vendored website-cloner scaffold's bundled agent files
!resources/website-cloner-scaffold/.claude/
!resources/website-cloner-scaffold/.cursor/

# Rust / Tauri build output (11GB+ — never commit)
src-tauri/target

# Swift / Xcode (archives-companion) — generated by xcodegen, build dirs
.build
DerivedData
*.xcodeproj
.swiftpm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brainstorm visual companion (local mockups)
.superpowers/
